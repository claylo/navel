# navel

Introspection toolkit for Claude Code internals. Tracks commands, hooks, feature flags, and documentation across every published npm version — by reverse-engineering a 12MB minified JavaScript file with bash, jq, and ripgrep.
